<IfModule mod_rewrite.c>
    RewriteEngine on
    # Redirigir la raíz a public/
    RewriteRule ^$ public/ [L]
    # Redirigir cualquier otra petición a public/
    RewriteRule (.*) public/$1 [L]
</IfModule>